package com.craft.apps.countdowns.common.database;

/**
 * A data source that handles more high-level tasks like interactions between the {@link
 * CountdownRepository} and {@link UserRepository}.
 *
 * @version 1.0.0
 * @since 2.0.0
 */
public class AppDatabase {

    private AppDatabase() {
        // no-op
    }
}
